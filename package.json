{
  "name": "pit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=26.4.0"
  },
  "scripts": {
    "test": "scripts/check-file-size.sh && pnpm typecheck && node --test --test-reporter=dot 'packages/*/src/**/*.test.ts'",
    "typecheck": "tsc --noEmit -p packages/tui/tsconfig.json && tsc --noEmit -p packages/app/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
