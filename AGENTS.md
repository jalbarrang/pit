# Agent Context

## Design Context

Strategic product context lives in [PRODUCT.md](PRODUCT.md) (register: product; platform: terminal; users, positioning, design principles). The visual system lives in [DESIGN.md](DESIGN.md) ("The Night Console": semantic theme roles, tint-ladder elevation, glyph vocabulary, do's and don'ts). Read both before designing or changing any UI surface. Theme tokens are defined only in `packages/app/src/domain/theming/themes/*.json`; components must use semantic roles, never raw hex.
